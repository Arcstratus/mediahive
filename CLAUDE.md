# Mediachive

## !!Important!!

!!絕對禁止使用簡體字以及中國用語!!

1. Do not edit pyproject.toml and packages.json to manage packages
2. use `uv` to manage backend (python) packages
3. use `pnpm` to manage frontend (vue) packages
4. 除非沒有其他方法，不然不要「前端」使用 query 路由 （`?q=ooxx`）

## References

- Nuxt UI: https://ui.nuxt.com/llms.txt — 不清楚 Nuxt UI 元件用法或 API 時，使用 WebFetch 查詢此連結
